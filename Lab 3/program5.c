#include <math.h>
#include <stdio.h>

void main(){
    
    int a,b,c,d;

    printf("Write the numbers to be swapped: ");
    scanf("%d %d %d %d", &a, &b, &c, &d);
    printf("\nYour chosen numbers are: %d %d %d %d", a,b,c,d);

    if(a>b){
        
    }
}